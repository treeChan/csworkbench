# v0.4.5 Release Notes

> 自 v0.4.4 以来的正式版更新。**强烈推荐更新**。

## 软件更新流程（完整版）⭐

- 内置自动更新：启动时后台检查，发现新版弹窗提醒（含更新日志）。
- 下载体验：真实进度 + 速度 + 预计剩余时间；可取消、可最小化到底部状态栏继续下载。
- 断网自动停止并提示重试，不再卡死。
- 正式 / 预览双渠道：加入「预览计划」可提前体验预览版，随时退出。

## 思维导图（初步）

- 容器节点、笔记 Markdown 渲染、搜索高亮。
- 连线改为直接拖拽锚点；节点可自定义填色 / 字色；PNG 导出修复。

## 界面

- 设置页统一左标签右值布局；数据库健康状态实时显示；底栏顺序优化。

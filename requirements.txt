# Workbench 运行依赖（浏览器版）
# 用 ./run.sh (Mac/Linux) 或 run.bat (Windows) 启动时会自动安装,
# 一般不需要手动执行 pip install。
#
# 桌面 App（desktop/ 目录）用的是另一份独立清单:
#   desktop/sidecar/requirements-desktop.txt
# 那里刻意不用 uvicorn[standard]（避开 C 扩展,便于 PyInstaller 打包）。
# PyInstaller 本身是构建工具,由 CI 单独安装,不在这份清单里。

fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
sqlalchemy>=2.0
python-multipart>=0.0.12
markdown>=3.7
pydantic>=2.10
pydantic-settings>=2.7
# 设置页写回 .env（持久化用户设置）需要
python-dotenv>=1.0

# 仅 scripts/example_log.py(从训练脚本推送数据的示例)需要。
# 不跑那个示例的话用不上,但装着也无妨。
requests>=2.31
